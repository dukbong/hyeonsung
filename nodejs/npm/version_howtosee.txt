SemVer 버저닝

노드 패키지의 버전은 SemVer(유의적 버저닝) 방식을 따른다.

구성요소 : Major(주버전).Minor(부버전).Patch(수버전)

    - 노드에서는 배포를 할 때 항상 버전을 올려야한다.
    - Major는 하위버전과 호환되지 않는 수정사항이 생겼을때 올린다.
    - Minor는 하위 버전과 호환되는 수정사항이 생겼을때 올린다.
    - Patch는 기능에 버그를 해결했을 때 올린다.